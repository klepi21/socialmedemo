import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AI ChatBot Demo | Next-Gen Sales Assistant",
  description: "Experience the future of sales with our AI-powered chatbot. Real-time voice interaction, smart lead analysis, and automated business proposals.",
  openGraph: {
    title: "AI ChatBot Demo",
    description: "Next-gen AI Sales Assistant with Voice and RAG capabilities.",
    images: ['/og-image.png'],
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
